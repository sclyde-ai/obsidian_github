# class
- np.ndarry
    - properties
        - dtype
        - ndim
        - shape

# function
- np.array(object)
    objectのnumpyを作成する
- np.zeros(shape)
    0の要素のみの行列を作成する
- np.ones(shape)
    1の要素のみの行列を作成する
- np.identity(n)
    単位行列を作成する
np.full(shape, value)

